# Test-side RAGas Notes

`LoveAppRagEvalCaptureTest` prefers dataset file `eval/datasets/loveapp_ragas_v1.jsonl`.
If that file is unavailable, you can place a backup dataset here as:

- `src/test/resources/ragas/loveapp_ragas_v1.jsonl`

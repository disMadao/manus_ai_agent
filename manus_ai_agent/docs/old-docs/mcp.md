因为家里网络加上梯子都挂了，这个小问题弄了我两天，老是因为自己的mcp服务器启动不行和高德的mcp服务的问题，在测试的时候要么连不上，要么报错格式不对。

后面发现是这两个都是通过stdio方式的，而日志输出也是输出在这里的，后面把图片搜索的mcp服务器的日志输出到了stderr，在高德的mcp连接命令中加了一个 --silent命令就解决了。


我的 Java 代码和 npx 包之间是 MCP 协议通信，通过stdio方式，而 npx 包和远程高德地图服务之间是 HTTP API 调用。

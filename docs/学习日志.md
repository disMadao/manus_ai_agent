2-26
看了两节视频，现在看视频真的没耐心了，效果不好，他也不能自定义多倍速，按f12也会自动关闭。之后看能不能改。

知道了四种接入ai模型的方法，sdk, http(curl)，spring ai, langchain4j。   spring ai里面有一个很反之前常识的地方，它在自动注入 ChatModel 的时候就是通过名字注入的！！！
比如下面就是注入的灵犀的模型，换成ollamaChatModel就是ollama部署的模型了。
@Resource
private ChatModel dashscopeChatModel;

还知道了powershell和linux能识别多行的命令。

然后是一些提示词技巧，示例，边界，之类的东西。

Spring ai 实现多轮对话的几种方式， 推荐的 ChatClient ，使用这个还带了 adivors 类似之前学过的拦截器，可以做关键词替换，优化prompt之类的。比如有 ragAdivsor， memoryAdivsor，多个顾问之间有优先级，可代码顺序无关，有内部的一个int变量order指定。

这里学到了一个看代码结构的方法，在idea中点进一个实现好的接口中，右键查看digram之类的图，进入后打开方法显示可以更好的查看。

chat memory 定义了对消息进行管理的几个关键操作，spring ai提供了几种方案存储消息：内存、jdbc数据库之类的，也可以自己实现存储消息的方法。

2-27
实现一个基于文件的序列化类，继承spring ai的ChatMemory，内部引入 kryo 实现序列化。

开始rag部分，现在开始不看视频了，看看文档就好了。

看了一半rag内容，感觉鱼皮的这个教程更类型一个框架教学，没有学习的意义，现在有了ai了。直接开始简历编写吧。
晚上还爬了一个小红书用户的笔记。准备做点有趣的东西。cursor当爬虫真的烧钱，下次还是得用老办法，用deepseek写提取脚本。省掉去除登录的部分，直接手动复制 dom 节点会减少很多脚本编码难度。

2-28
rag没什么好学习的，看完了，只知道一些基本流程。

工具调用，spring ai的两种方式 @tool 和 注册成bean ，底层都是转换成 Callback。还是得 clone 源码学习。

看完了项目的工具调用和MCP篇章。

